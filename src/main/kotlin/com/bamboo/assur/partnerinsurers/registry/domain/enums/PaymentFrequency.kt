package com.bamboo.assur.partnerinsurers.registry.domain.enums

/**
 * Represents the frequency at which payments are made for an insurance policy.
 *
 * This enum defines the standard payment frequencies supported by the system.
 * Each value corresponds to a specific interval for recurring payments.
 */
enum class PaymentFrequency { DAILY, WEEKLY, MONTHLY, QUARTERLY, SEMI_ANNUALLY, ANNUALLY }