name: Release

on:
  push:
    branches:
      - main
      - develop
      - 'release/**'
      - 'hotfix/**'
      - 'feature/**'
      - 'fix/**'

  workflow_dispatch:

env:
  GPR_USER: ${{ secrets.GPR_USER }}
  GPR_KEY: ${{ secrets.GPR_KEY }}

jobs:
  ci-checks:
    uses: ./.github/workflows/ci.yml
    secrets:
      PACKAGES_TOKEN: ${{ secrets.PACKAGES_TOKEN }}

  release:
    needs: ci-checks
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, 'skip ci')"
    
    permissions:
      contents: write
      issues: write
      pull-requests: write
      packages: read

    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}

      # Minimal Java setup for semantic-release Gradle build
    - name: Set up JDK 21
      uses: actions/setup-java@v5
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22.x'
        cache: npm

    - name: Install dependencies
      run: npm ci

    - name: Semantic Release
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GPR_USER: ${{ secrets.GPR_USER }}
        GPR_KEY: ${{ secrets.GPR_KEY }}
        ORG_GRADLE_PROJECT_gpr_user: ${{ secrets.GPR_USER }}
        ORG_GRADLE_PROJECT_gpr_key: ${{ secrets.GPR_KEY }}
        ORG_GRADLE_PROJECT_githubPackagesRepo: https://maven.pkg.github.com/billionaire-devs/partner-insurers-shared-kernel
      run: npx semantic-release
