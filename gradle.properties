# Project-wide Gradle settings
org.gradle.jvmargs = -Xmx1g
projectName = registry
# Managed by semantic-release-gradle. Do not edit manually.
version = 1.0.0-rc-1-0-0.1
# Credentials are intentionally NOT stored here.
# For CI, provide via environment variables (GPR_USER / GPR_KEY) or GitHub Actions secrets.
# For local dev, create an untracked file `gradle.local.properties` with:
#   gpr.user=YOUR_USERNAME
#   gpr.key=YOUR_TOKEN
